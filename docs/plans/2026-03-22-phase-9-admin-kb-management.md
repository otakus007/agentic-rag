# Phase 9: Admin KB Management

**Goal:** Create Admin interface for managing Knowledge Bases (KB), including drag-and-drop PDF ingestion capabilities.

## Tasks
1. Route guards for `AdminView`.
2. KB List View listing all vector search agents.
3. `FileUploadZone.vue` taking PDF buffers to send to the `/ingest` endpoints.
4. Display ingestion success/errors.
