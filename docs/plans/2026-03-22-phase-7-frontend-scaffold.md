# Phase 7: Frontend Scaffold & Design System

**Goal:** Initialize Vue 3, Vite, and Tailwind v4 environment. Implement base routing and layout components.

## Tasks
1. Initialize Vue 3 Vite app with Tailwind v4.
2. Establish Dark OLED design system base layout.
3. Setup `vue-router` for `Login`, `Chat`, and `Admin` views.
4. Implement common components (`AppHeader`, `Sidebar`).
