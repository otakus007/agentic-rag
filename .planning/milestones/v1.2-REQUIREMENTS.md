# v1.2 Requirements: Production Polish — ARCHIVED

> **Archived:** 2026-03-22 | **Status:** DELIVERED (14/17 requirements met)

## Real-time UX
- [x] Dark/light mode toggle with system preference detection → `useTheme.js`
- [x] SSE streaming for chat responses (token-by-token display) → `POST /chat/stream`
- [ ] Ingestion progress tracking via SSE → **Deferred to v1.3** (document CRUD done)

## Multi-Provider LLM
- [x] Unified LLM adapter interface (OpenAI, Gemini, Anthropic) → `src/llm/adapters.py`
- [x] Dynamic model discovery from provider APIs → `get_all_models()`
- [x] Model routing: chatbot config selects provider + model → `_get_all_model_names()` validation
- [ ] Provider API key management (admin-configured) → **Deferred to v1.3** (keys via .env)

## Access Control
- [x] User management: list, update role → `GET/PUT /admin/users`
- [x] Role-based access: admin vs. user roles → `src/auth/rbac.py`
- [x] Admin-only routes protected by role check → `require_admin` dependency
- [ ] Microsoft OAuth provider → **Deferred to v1.3** (Google OAuth in place)

## Admin Enhancements
- [x] Document-level CRUD: view and delete chunks → `GET/DELETE /admin/kb/{id}/documents`
- [ ] KB usage analytics → **Deferred to v1.3**

## Technical Requirements
- [x] Backend SSE endpoint for streaming chat → `StreamingResponse`
- [x] Frontend SSE client with progressive text rendering → `useChat.js` ReadableStream
- [x] Database migration system → Auto-migration in lifespan (chatbots + users)
- [x] API versioning strategy → `GET /api/version` returns 1.2.0
