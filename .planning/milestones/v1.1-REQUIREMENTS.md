# v1.1 Requirements: Frontend UI — ARCHIVED

> **Archived:** 2026-03-22 | **Status:** DELIVERED (20/22 requirements met)

## User Chat Interface
- [x] Chat page with message input, conversation history, and streaming AI responses → `ChatView.vue`, `useChat.js`
- [x] Display inline citation markers `[1]` linked to source documents → `MessageBubble.vue`
- [x] Sources panel showing retrieved document chunks with page numbers → `SourcesPanel.vue`, `SourceCard.vue`
- [ ] Dark mode / light mode toggle → **Deferred to v1.2** (Dark OLED implemented as primary)
- [x] OAuth login flow (Google/Microsoft) with token storage → `LoginView.vue` + backend OAuth
- [x] Conversation list sidebar for session management → `ConversationSidebar.vue`
- [x] Responsive layout (mobile-friendly) → Sidebar collapses, sources overlay

## Admin: Knowledge Base Management
- [x] Dashboard showing list of knowledge bases (Qdrant collections) → `KBListView.vue`
- [x] Upload PDF files to create/update a knowledge base → `FileUploadZone.vue`
- [x] View ingestion status and document count per KB → `GET /admin/kb`
- [x] Delete knowledge base → `DELETE /admin/kb/{agent_id}`

## Admin: Chatbot Builder
- [x] Create chatbot with name, description, assigned KB, and LLM model selection → `POST /admin/chatbots`
- [x] List existing chatbots with status → `ChatbotListView.vue`
- [x] Edit chatbot configuration (KB assignment, model) → `PUT /admin/chatbots/{id}`
- [x] Test chat panel within admin to verify chatbot behavior → Inline test chat in edit modal

## Technical Requirements
- [x] Vue 3 + Vite + Tailwind CSS frontend scaffold → `frontend/` with Tailwind v4
- [x] UUPM (ui-ux-pro-max-skill) design system integration → `style.css` @theme tokens
- [x] API client module for backend communication (`/chat`, `/ingest`, `/health`) → `api/client.js`
- [x] JWT token management (store, refresh, attach to API calls) → `api/auth.js`
- [x] Monorepo structure: `frontend/` directory alongside existing `src/` → Separate `package.json`

## Out of Scope (v1.1) → Deferred to v1.2
- Multi-LLM routing (v1.2)
- User management / role-based access control (v1.2)
- Real-time WebSocket streaming (use SSE or polling first)
